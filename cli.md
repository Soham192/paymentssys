To go back to the previous directory in the CLI, you can use:cd -
To go one directory back (up) in the CLI, use: cd .. //remember to use space

mkdir for folder and touch for file
rename- mkdir oldfilename newfilename